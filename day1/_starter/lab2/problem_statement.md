# Day 1 — Lab 2: Extract Error Details (Backend Habit)

## Goal
Write a tiny helper that turns an HTTP error response into a clean, useful message.

## What you’ll practice
- Defensive parsing of JSON responses
- Fallback logic (error → message → raw text)

## Acceptance criteria
- `python -m pytest -q day1/lab2/tests` passes

## Constraints
- Keep this lab self-contained in this folder.
- Do not import code from other days.
