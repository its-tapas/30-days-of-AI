You are a Senior AI Engineer + strict reviewer.

Context (read carefully)
- I am an absolute beginner: I don’t know AI/ML/LLM basics.
- I have NO paid API keys.
- I want everything free on my personal Windows laptop.
- Goal: build real AI systems in 30 days by coding (2–3 hours/day max).
- Repo rules:
  - One evolving project in repo root: app/
  - Each day also has an independent demo: dayX/app1/
  - Dependencies go into requirements.txt
  - Use a repo-wide virtual env: .venv/

Task (Day 1)
Help me build my first working AI app:
1) Run a local LLM for free (Ollama)
2) Call it from Python via HTTP
3) Build an interactive CLI prompt runner
4) Build an independent mini-demo in day1/app1/ (separate code)

Hard requirements
- Be practical. No long theory.
- Before each demo step, give me a 2–5 minute “mini-theory” I can say out loud.
- Give step-by-step PowerShell commands.
- After EACH major step, ask me to paste:
  1) the command(s) I ran
  2) the output
  3) any errors
- Keep it within 2–3 hours.

Provider choice
- Use Ollama locally (no API keys).

What to build (deliverables)
A) app/llm_client.py
- Reads env vars:
  - OLLAMA_BASE_URL (default http://localhost:11434)
  - OLLAMA_MODEL (example llama3.2:3b)
- Function: generate_text(user_prompt: str, system_prompt: str | None = None) -> str
- Calls: POST {OLLAMA_BASE_URL}/api/generate
  - JSON body includes: model, prompt, stream=false
- Handles errors clearly:
  - Ollama not running (connection refused)
  - model missing (tell me to `ollama pull <model>`)

B) app/cli_prompt.py
- Interactive loop
- user types prompt
- prints answer
- exit quits

C) .env.example
- Safe defaults, no secrets

D) Independent day demo (day1/app1/)
- A minimal script like day1/app1/ollama_one_shot.py that:
  - does NOT import from app/
  - calls Ollama once and prints the response

Validation
- Provide 3 tests:
  1) happy path prompt
  2) Ollama not running
  3) wrong model name

Start by asking me ONLY these 3 questions:
1) Do I have winget available? (yes/no/not sure)
2) Which model should we start with: llama3.2:3b or gemma2:2b?
3) Confirm: we will create ONE .venv at repo root for everything (yes/no)

Then proceed with the smallest workable implementation.